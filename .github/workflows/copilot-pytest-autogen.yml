name: Copilot-Pytest-AutoGen

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  ai-test-gen:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write

    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PYTHONPATH: .

    steps:
      # 🧱 1️⃣ Checkout full repository history (so git diff works)
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}

      # 🗂️ 2️⃣ Ensure directory structure exists
      - name: Ensure folder structure
        run: |
          mkdir -p src tests .github/scripts
          echo "📂 Directory structure ready"

      # 🤖 3️⃣ Install GitHub CLI and Copilot extension
      - name: Install GitHub CLI and Copilot CLI extension
        run: |
            sudo apt-get update -y
            sudo apt-get install -y jq
            echo "🧩 Installing new GitHub Copilot CLI extension..."
            gh extension remove github/gh-copilot-cli || true
            gh extension install github/gh-copilot-cli
            gh extension upgrade github/gh-copilot-cli || true
            gh --version
            echo "✅ Copilot CLI installed successfully"



      # 🐍 4️⃣ Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 📦 5️⃣ Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov

      # 🧠 6️⃣ Verify generator script exists in repo
      - name: Verify generator script
        run: |
          if [ ! -f ".github/scripts/copilot_test_gen.py" ]; then
            echo "❌ Missing .github/scripts/copilot_test_gen.py. Please commit it before running this workflow."
            exit 1
          fi
          echo "✅ Generator script found in repo."

      # ⚡ 7️⃣ Run Copilot-based test generation
      - name: Run Copilot Test Generator
        run: |
            # Run generator + tests, keep pytest output for comment parsing
            python .github/scripts/copilot_test_gen.py
            echo "📊 Running pytest with coverage..."
            pytest -q --disable-warnings --maxfail=1 \
            --cov=src --cov-report=term --cov-report=xml \
            | tee pytest_output.log


      # 🧾 8️⃣ Post PR Comment Summary (with test counts + coverage)
      - name: Post Copilot Test Summary on PR
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR_NUMBER=$(jq -r .pull_request.number < "$GITHUB_EVENT_PATH")
          STATUS="${{ job.status }}"
          echo "🔍 Collecting pytest results..."

          TESTS_PASSED=0
          TESTS_FAILED=0
          PLACEHOLDER_COUNT=0
          COVERAGE="N/A"

          # --- Parse pytest summary if present ---
          if [ -f pytest_output.log ]; then
            TESTS_PASSED=$(grep -Eo "[0-9]+ passed" pytest_output.log | awk '{print $1}' | tail -n1 || echo 0)
            TESTS_FAILED=$(grep -Eo "[0-9]+ failed" pytest_output.log | awk '{print $1}' | tail -n1 || echo 0)
            COVERAGE_LINE=$(grep -E "TOTAL\s+[0-9]+\s+[0-9]+\s+[0-9]+\s+[0-9]+%" pytest_output.log | tail -n1)
            if [ -n "$COVERAGE_LINE" ]; then
              COVERAGE=$(echo "$COVERAGE_LINE" | grep -Eo "[0-9]+%$" | tr -d '%')
            fi
          fi

          # --- Count placeholder tests ---
          PLACEHOLDER_COUNT=$(grep -R "test_placeholder" tests/ 2>/dev/null | wc -l || echo 0)

          # --- Build result text ---
          if [ "$STATUS" = "success" ] && [ "$TESTS_FAILED" -eq 0 ]; then
            if [ "$PLACEHOLDER_COUNT" -gt 0 ]; then
              RESULT="⚠️ $TESTS_PASSED passed, 0 failed ($PLACEHOLDER_COUNT placeholder)."
            else
              RESULT="✅ $TESTS_PASSED passed, 0 failed."
            fi
          elif [ "$TESTS_FAILED" -gt 0 ]; then
            RESULT="❌ $TESTS_FAILED failed, $TESTS_PASSED passed."
          else
            RESULT="❌ Copilot test generation or pytest validation failed."
          fi

          # --- Append coverage if numeric ---
          if [ "$COVERAGE" != "N/A" ]; then
            RESULT="$RESULT Coverage: ${COVERAGE}%."
          fi

          BODY="🤖 **Copilot Test Automation Summary**  
          • Status: **$STATUS**  
          • Result: $RESULT  
          _Check workflow logs for details._"

          echo "🧾 Posting comment to PR #$PR_NUMBER ..."
          gh pr comment "$PR_NUMBER" --body "$BODY"

    # # 🧾 8️⃣ Post PR Comment Summary
    #   - name: Post Copilot Test Summary on PR
    #     if: always()
    #     env:
    #       GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    #     run: |
    #       PR_NUMBER=$(jq -r .pull_request.number < "$GITHUB_EVENT_PATH")
    #       STATUS="${{ job.status }}"
    #       if grep -q "✅ Tests passed" "${GITHUB_WORKSPACE}/.github/scripts/copilot_test_gen.py" 2>/dev/null; then
    #         SUMMARY="✅ Copilot test generation succeeded and pytest passed."
    #       elif grep -q "⚠️ Copilot did not generate" "${GITHUB_WORKSPACE}/.github/scripts/copilot_test_gen.py" 2>/dev/null; then
    #         SUMMARY="⚠️ Copilot generated placeholder tests (CLI returned no output)."
    #       else
    #         SUMMARY="❌ Copilot test generation or pytest validation failed."
    #       fi
    #       BODY="🤖 **Copilot Test Automation Summary**  
    #       • Status: **$STATUS**  
    #       • Result: $SUMMARY  
    #       _Check workflow logs for details._"
    #       gh pr comment "$PR_NUMBER" --body "$BODY"
    

      # 🧾 8️⃣ (Optional) Post result summary comment on PR
      # Uncomment below if you want Copilot test summary posted to PR
      #
      # - name: Post PR Comment Summary
      #   if: always()
      #   run: |
      #     PR_NUMBER=$(jq -r .pull_request.number < "$GITHUB_EVENT_PATH")
      #     STATUS="${{ job.status }}"
      #     BODY="🤖 **Copilot Test Generation Result:** $STATUS\nCheck workflow logs for details."
      #     gh pr comment "$PR_NUMBER" --body "$BODY"
