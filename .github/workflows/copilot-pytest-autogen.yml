name: Copilot-Pytest-AutoGen

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  ai-test-gen:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Ensure folder structure
        run: |
          mkdir -p src tests .github/scripts
          echo "📂 Directory structure ready"

      - name: Install GitHub Copilot CLI
        run: |
          echo "🧩 Installing GitHub Copilot CLI via npm..."
          sudo apt-get update -y
          sudo apt-get install -y npm jq
          sudo npm uninstall -g @githubnext/github-copilot-cli || true
          sudo npm install -g @githubnext/github-copilot-cli
          echo "$(npm bin -g)" >> $GITHUB_PATH
          which github-copilot-cli
          github-copilot-cli --help | head -20 || true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-cov coverage

      - name: Verify generator script
        run: |
          if [ ! -f ".github/scripts/copilot_test_gen.py" ]; then
            echo "❌ Missing copilot_test_gen.py"
            exit 1
          fi
          echo "✅ Generator script found."

      - name: Run Copilot Test Generator
        run: |
          echo "🚀 Starting Copilot Test Generator..."
          python .github/scripts/copilot_test_gen.py

      - name: Post summary
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "✅ Copilot-based pytest generation completed."
